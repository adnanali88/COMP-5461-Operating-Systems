Assignment 2 - Questions

Q1. In Phase(i), explain why you chose synchronized methods or synchronized statements to protect the critical section.

Implementing Synchronize methods would be inefficient since multi-threading cannot invoke the method when a synchronize method is processing. 
Instead, synchronized statements were implemented to ensure consistent output as well efficiency.
